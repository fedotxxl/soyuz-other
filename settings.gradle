include(
        [
                "concurrent", "crypto", "err", "exec", "func", "jongo", "log",
                "mail", "metrics-prometheus", "queue", "queue-domain", "template",
                "validator", "validator-spring", "validator-jax-rs", "validator-to-err"
        ]
                .collect { ":soyuz-${it}" } as String[])