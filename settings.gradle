include(
        [
                "bus",
                "concurrent", "crypto",
                "err", "exec",
                "func",
                "io",
                "jongo",
                "log",
                "mail", "metrics-prometheus",
                "queue", "queue-domain",
                "template",
                "ua",
                "validator", "validator-spring", "validator-jax-rs", "validator-to-err"
        ]
                .collect { ":soyuz-${it}" } as String[])